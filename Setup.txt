Ensure you have created the environment in the directory where you will be working
python -m venv .venv

Before starting to work, you need to activate it
.venv\Scripts\activate  (Windows)
source .venv/bin/activate (Linux)

install the requirements in requirements.txt